# Plan: <feature name>

> HOW we will implement the spec. Alternatives, trade-offs, file map.

## Approach

<2-4 paragraphs describing the chosen approach.>

## Alternatives considered

- **<Alternative A>** — <one-line summary>. Rejected because <reason>.
- **<Alternative B>** — <one-line summary>. Rejected because <reason>.

## File map

| Path                                          | Action | Purpose                          |
|-----------------------------------------------|--------|----------------------------------|
| `src/<path>`                                  | create | <what it does>                   |
| `src/<path>`                                  | edit   | <what changes>                   |
| `tests/e2e/<name>.spec.ts`                    | create | <E2E coverage for criterion X>   |

## Test plan

- **Unit/component** (Vitest): <list the tests you'll write>.
- **E2E** (Playwright): <list the user journeys>.

## Risks

- <Risk> — mitigation: <action>.
